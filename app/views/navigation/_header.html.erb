<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">Showoff Widgets</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span	 class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%if user_signed_in?%>
            <%= link_to 'New Widget', new_widget_path, class: "nav-link" %>
        <% end -%>
      </li>
    </ul>
    <form class="form-inline my-2 my-lg-0">
      <ul class="navbar-nav mr-auto">
        <%if user_signed_in?%>
          <li class="nav-item"><%= link_to("My Widgets", my_widget_path, class: "nav-link") %></li>
          <li class="nav-item"><%= link_to(current_user.first_name, "/users/#{current_user.showoff_user_id}?user_info=true", class: "nav-link") %></li>
          <li class="nav-item"><%= link_to('Logout', destroy_user_session_path, method: :delete, class: "nav-link") %></li>
        <%else%>
          <li class="nav-item"><a href="/users/sign_up" class="nav-link"><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>
          <li class="nav-item"><a href="/users/sign_in" class="nav-link"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
        <%end%>
      </ul>
    </form>
    <%= form_tag(search_path(:my_search => controller.action_name), method: :post, :class => "form-inline my-2 my-lg-0 search_button") do %>
      <%= text_field_tag(:search, params[:search], :class => "form-control mr-sm-2", :placeholder=>"Search Widgets" ) %>
      <%= submit_tag("Search", :class=>"btn btn-outline-success my-2 my-sm-0") %>
    <% end %>
  </div>
</nav>
